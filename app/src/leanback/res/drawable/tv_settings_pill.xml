<?xml version="1.0" encoding="utf-8"?>
<!-- Round icon-only button (gear).
     Used for the standalone settings gear on the right side of the topnav. -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_focused="true">
        <shape android:shape="oval">
            <solid android:color="@color/tv_glass" />
            <stroke android:width="@dimen/tv_focus_width" android:color="?attr/tvColorFocus" />
            <stroke android:width="@dimen/tv_glow_band" android:color="?attr/tvColorFocusGlow" />
        </shape>
    </item>
    <item>
        <shape android:shape="oval">
            <solid android:color="@color/tv_glass" />
            <stroke android:width="1dp" android:color="?attr/tvColorOutline" />
        </shape>
    </item>
</selector>