<?xml version="1.0" encoding="utf-8"?>
<!-- Accent pill at 25% alpha (empty-state top badge background). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/tv_accent_quarter" />
    <corners android:radius="999dp" />
    <stroke
        android:width="1dp"
        android:color="@color/tv_accent_half" />
</shape>